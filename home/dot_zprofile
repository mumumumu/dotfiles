if [[ "$(uname -s)" == "Linux" ]]; then
    eval "$(keychain --quiet --eval --agents ssh id_rsa)"
fi

if [[ "$(uname -s)" == "Darwin" ]]; then
    eval "$(/opt/homebrew/bin/brew shellenv)"
    eval "ssh-add -q --apple-use-keychain ~/.ssh/id_rsa"
fi

